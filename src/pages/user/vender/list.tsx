import { DOLayout } from "layouts";
import { NextPage } from "next";

const VenderList: NextPage = () => {
  return (
    <DOLayout title="Vender | List" appBarTitle="Vender">
      <div>Vender List</div>
    </DOLayout>
  );
};

export default VenderList;
